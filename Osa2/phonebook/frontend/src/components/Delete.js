/*import React from 'react';

const Delete = (props) => {

   const person = props.person
   
    return (
        <button onClick={() => {
            delete(person.id)
        }}>
            delete
        </button>
    )
}

export default Delete;*/